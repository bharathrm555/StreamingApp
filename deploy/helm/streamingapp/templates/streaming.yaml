apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamingapp-streaming-brm
  labels:
    {{- include "streamingapp.labels" . | nindent 4 }}
    app: streamingapp-streaming-brm
spec:
  replicas: {{ .Values.services.streaming.replicaCount }}
  selector:
    matchLabels:
      app: streamingapp-streaming-brm
  template:
    metadata:
      labels:
        app: streamingapp-streaming-brm
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: streaming
          image: {{ include "streamingapp.image" (dict "root" . "component" "streaming") }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.streaming.containerPort }}
          env:
            - name: PORT
              value: {{ .Values.services.streaming.containerPort | quote }}
            - name: MONGO_URI
              value: {{ .Values.global.mongoUri | quote }}
            - name: CLIENT_URLS
              value: {{ join "," .Values.global.clientUrls | quote }}
            - name: STREAMING_PUBLIC_URL
              value: {{ (first .Values.global.clientUrls) | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: streamingapp-secrets-brm
                  key: JWT_SECRET
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: streamingapp-secrets-brm
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: streamingapp-secrets-brm
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_REGION
              valueFrom:
                secretKeyRef:
                  name: streamingapp-secrets-brm
                  key: AWS_REGION
            - name: AWS_S3_BUCKET
              valueFrom:
                secretKeyRef:
                  name: streamingapp-secrets-brm
                  key: AWS_S3_BUCKET
            - name: AWS_CDN_URL
              valueFrom:
                secretKeyRef:
                  name: streamingapp-secrets-brm
                  key: AWS_CDN_URL
          readinessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.services.streaming.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /api/health
              port: {{ .Values.services.streaming.containerPort }}
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            {{- toYaml .Values.services.streaming.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: streamingapp-streaming-brm
  labels:
    {{- include "streamingapp.labels" . | nindent 4 }}
    app: streamingapp-streaming-brm
spec:
  type: ClusterIP
  selector:
    app: streamingapp-streaming-brm
  ports:
    - name: http
      port: {{ .Values.services.streaming.servicePort }}
      targetPort: {{ .Values.services.streaming.containerPort }}
